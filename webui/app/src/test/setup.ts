import { beforeAll } from "vitest";
import "@testing-library/jest-dom";

// Setup for SolidJS testing
beforeAll(() => {
  // Any global test setup can go here
  // For example, mocking global APIs, setting up test utilities, etc.
});
